<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kitchenkompanion" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/main_icon_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/main_icon_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.KitchenKompanion.NoActionBar" >
        <activity
            android:name=".RecipeList"
            android:exported="true" />
        <activity
            android:name=".AddRecipeActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.ADDRECIPE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UserItemList"
            android:exported="true" />
        <activity
            android:name=".ShoppingList"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>